name: Release

on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+*"

permissions:
  contents: read

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

env:
  UV_VERSION: "0.12.10"

jobs:
  build:
    name: Validate and build
    runs-on: ubuntu-latest
    timeout-minutes: 25
    outputs:
      prerelease: ${{ steps.release.outputs.prerelease }}
      version: ${{ steps.release.outputs.version }}
    steps:
      - name: Checkout complete history
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Validate release tag
        id: release
        env:
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          if [[ ! "$RELEASE_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+((a|b|rc)[0-9]+)?$ ]]; then
            echo "Invalid release tag: $RELEASE_TAG" >&2
            exit 1
          fi
          git fetch --no-tags origin main
          if ! git merge-base --is-ancestor "$GITHUB_SHA" origin/main; then
            echo "Release tag must point to a commit on main" >&2
            exit 1
          fi
          if [[ "$RELEASE_TAG" =~ (a|b|rc)[0-9]+$ ]]; then
            echo "prerelease=true" >> "$GITHUB_OUTPUT"
          else
            echo "prerelease=false" >> "$GITHUB_OUTPUT"
          fi
          echo "version=${RELEASE_TAG#v}" >> "$GITHUB_OUTPUT"
      - name: Set up Python and locked dependencies
        uses: ./.github/actions/python-uv
        with:
          uv-version: ${{ env.UV_VERSION }}
          python-version: "3.10"
      - name: Analyze
        run: uv run --locked python -m scripts.analyze
      - name: Run unit tests
        run: uv run --locked python -m scripts.tests
      - name: Build distributions
        run: uv build --clear
      - name: Verify distributions
        env:
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
        run: >-
          uv run --locked python -m scripts.verify_release dist
          --expected-version "$RELEASE_VERSION"
      - name: Install and verify wheel
        env:
          RELEASE_VERSION: ${{ steps.release.outputs.version }}
        run: |
          uv venv "$RUNNER_TEMP/release-venv" --python 3.10
          uv pip install --python "$RUNNER_TEMP/release-venv/bin/python" dist/*.whl
          "$RUNNER_TEMP/release-venv/bin/kantrip" --version | grep -F "$RELEASE_VERSION"
      - name: Generate release notes
        uses: orhun/git-cliff-action@v4.9.1
        with:
          version: v2.14.1
          config: cliff.toml
          args: --current --strip header --output release-notes.md
          github_token: ${{ secrets.GITHUB_TOKEN }}
      - name: Upload release bundle
        uses: actions/upload-artifact@v7.0.1
        with:
          name: release-bundle
          path: |
            dist/
            release-notes.md
          if-no-files-found: error
          retention-days: 7

  attest-packages:
    name: Attest packages
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      attestations: write
    steps:
      - name: Download release bundle
        uses: actions/download-artifact@v8.0.1
        with:
          name: release-bundle
      - name: Attest distributions
        uses: actions/attest@v4.2.2
        with:
          subject-path: dist/*

  e2e-tests:
    name: E2E / exact release wheel
    needs: build
    uses: ./.github/workflows/e2e.yml
    with:
      candidate-artifact: release-bundle

  publish-pypi:
    name: Publish to PyPI
    needs: [build, attest-packages, e2e-tests]
    runs-on: ubuntu-latest
    environment:
      name: release
      url: https://pypi.org/project/kantrip/${{ needs.build.outputs.version }}/
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Download release bundle
        uses: actions/download-artifact@v8.0.1
        with:
          name: release-bundle
      - name: Publish distributions
        uses: pypa/gh-action-pypi-publish@v1.14.2
        with:
          packages-dir: dist/

  github-release:
    name: Publish GitHub release
    needs: [build, publish-pypi]
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
    steps:
      - name: Checkout release tag
        uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Download release bundle
        uses: actions/download-artifact@v8.0.1
        with:
          name: release-bundle
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          IS_PRERELEASE: ${{ needs.build.outputs.prerelease }}
          RELEASE_TAG: ${{ github.ref_name }}
        run: |
          release_args=()
          if [[ "$IS_PRERELEASE" == "true" ]]; then
            release_args+=(--prerelease)
          fi
          gh release create "$RELEASE_TAG" dist/* \
            --title "$RELEASE_TAG" \
            --notes-file release-notes.md \
            --verify-tag \
            --fail-on-no-commits \
            "${release_args[@]}"
